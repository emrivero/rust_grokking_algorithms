use std::collections::HashMap;

pub fn dijkstra(
    graph: &HashMap<String, Vec<String>>,
    node: &String,
    finish_node: &String,
    visited_param: Option<&Vec<&String>>, /* = "!" */
) -> Vec<String> {
    vec![]
}
